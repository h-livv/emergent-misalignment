# Frozen

Run 3 is archived at `outputs/archive/run3-2026-08-30/`. This live directory is not the experiment.

Do not recreate `raw/` here. New jobs should use `EM_RUN=run4` (or another unused name).
